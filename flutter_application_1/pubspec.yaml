name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # É uma boa prática definir um range maior

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # Pacotes necessários para a nova arquitetura
  http: ^1.2.1
  table_calendar: ^3.1.2
  
  # Pacotes de ícones e outros
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # Versão mais recente pode ser 4.0.0 ou 5.0.0

flutter:
  uses-material-design: true

